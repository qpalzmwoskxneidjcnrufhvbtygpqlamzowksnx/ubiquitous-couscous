
1. Background Music finish
2. Settings (take another look at which ones we actually need vs what we can implement vs what we can remove)
3. FAQ
    - Menu that pops up with frequently asked questions, instructions on how to use the app, etc.
4. Test App on phone (NOT YET)

Specifications for Settings
- ActiveBuddyAnimations, wait until we have the buddy+ActiveBuddyAnimations

5. Instead of a shop, want to add objects that load in with the backgrounds 
    - For example, an office background might let the buddy stand on the table
    - Just 2d objects, so can simply have UICollidable hitboxes where we want them. 
    - Can do this through making the background itself a prefab? Or storing as data the positions + sizes to put the hitboxes?

- For notifications:
    - Package Unity.Notifications.Android to get up and running. Example from google: 



Notifications:
- for users to get off their phones
- picks from pool of messages to send every so often



Sound Effects Needed:
- Clicky SFX for UI Button click


Buddy Animations
- Walking - Plays whenever buddy walks
- Rotating - Plays whenever buddy turns direction
- Wave at screen - Perhaps plays randomly
- Sit on a button? - Perhaps plays randomly
- Lots of different Emotes

SFX
- Plays some depending on the Emote (music if dancing, little noises here and there)
- Emote music overrides background music temporarily 




using Unity.Notifications.Android;

// 1. Register Android Channel
var channel = new AndroidNotificationChannel()
{
    Id = "energy_channel",
    Name = "Game Reminders",
    Importance = Importance.Default,
    Description = "Reminds you when game events finish",
};
AndroidNotificationCenter.RegisterNotificationChannel(channel);

// 2. Schedule Notification
var notification = new AndroidNotification();
notification.Title = "Energy Refilled!";
notification.Text = "Jump back in and start playing!";
notification.FireTime = System.DateTime.Now.AddHours(2);

AndroidNotificationCenter.SendNotification(notification, "energy_channel");



